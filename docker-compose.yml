services:
  todo-app:
    build:
      context: .
      dockerfile: Dockerfile
    image: todo-app:v1.0
    container_name: todo-app
    ports:
      - "5000:5000"
    volumes:
      - ./app:/app
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=development
    restart: unless-stopped
